{
  "rules": {
    "admins": {
      "$userId": {
        ".read": "auth != null && auth.uid === $userId",
        ".write": false
      }
    },
    "rooms": {
      ".read": "auth != null",
      "$roomId": {
        ".write": "auth != null && (root.child('admins').child(auth.uid).val() === true || ((!data.exists() && newData.child('hostUid').val() === auth.uid) || (data.exists() && (data.child('hostUid').val() === auth.uid || data.child('guestUid').val() === auth.uid || (data.child('status').val() === 'waiting' && data.child('guestUid').val() == null && newData.child('guestUid').val() === auth.uid && newData.child('hostUid').val() === data.child('hostUid').val())))))"
      }
    },
    "roomPresence": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null && root.child('admins').child(auth.uid).val() === true",
        "$userId": {
          ".write": "auth != null && (auth.uid === $userId || root.child('admins').child(auth.uid).val() === true)"
        }
      }
    },
    "userSessions": {
      ".read": "auth != null && root.child('admins').child(auth.uid).val() === true",
      "$userId": {
        ".read": "auth != null && (auth.uid === $userId || root.child('admins').child(auth.uid).val() === true)",
        ".write": "auth != null && (auth.uid === $userId || root.child('admins').child(auth.uid).val() === true)"
      }
    }
  }
}
